<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SweetShopMa.Views.LoginPage"
             Title="Login"
             BackgroundColor="#f5f5f5">

    <VerticalStackLayout 
        Padding="20" 
        Spacing="20" 
        VerticalOptions="Center"
        HorizontalOptions="Center"
        MaximumWidthRequest="400">

        <!-- Language Selector -->
        <HorizontalStackLayout HorizontalOptions="End" Spacing="10">
            <Button Text="EN" 
                    x:Name="EnglishButton"
                    BackgroundColor="#32b8c6"
                    TextColor="White"
                    Padding="15,5"
                    CornerRadius="5"
                    FontSize="12"
                    Clicked="OnLanguageButtonClicked"/>
            <Button Text="AR" 
                    x:Name="ArabicButton"
                    BackgroundColor="#32b8c6"
                    TextColor="White"
                    Padding="15,5"
                    CornerRadius="5"
                    FontSize="12"
                    Clicked="OnLanguageButtonClicked"/>
        </HorizontalStackLayout>

        <!-- Header -->
        <Label 
            x:Name="AppTitleLabel"
            Text="🍬 Sweet Shop" 
            FontSize="32" 
            FontAttributes="Bold" 
            TextColor="#32b8c6"
            HorizontalTextAlignment="Center"/>
        <Label 
            x:Name="SecureLoginLabel"
            Text="🔐 Secure Login" 
            FontSize="18" 
            TextColor="#666"
            HorizontalTextAlignment="Center"/>

        <!-- Username Field -->
        <StackLayout Spacing="8">
            <Label x:Name="UsernameLabel" Text="👤 Username" FontSize="14" FontAttributes="Bold" TextColor="#333"/>
            <Entry 
                x:Name="UsernameEntry"
                Placeholder="Enter username"
                BackgroundColor="White"
                FontSize="14"
                ReturnType="Next"
                Completed="OnUsernameEntryCompleted"/>
        </StackLayout>

        <!-- Password Field with Lock Icon -->
        <StackLayout Spacing="8">
            <Label x:Name="PasswordLabel" Text="🔒 Password" FontSize="14" FontAttributes="Bold" TextColor="#333"/>
            <Entry 
                x:Name="PasswordEntry"
                IsPassword="True"
                Placeholder="Enter password"
                BackgroundColor="White"
                FontSize="14"
                ReturnType="Go"
                Completed="OnPasswordEntryCompleted"/>
        </StackLayout>

        <!-- Error Message -->
        <Label 
            x:Name="ErrorLabel"
            TextColor="#c00000"
            FontSize="12"
            Padding="10"
            BackgroundColor="#ffe6e6"
            HorizontalTextAlignment="Center"
            IsVisible="False"/>

        <!-- Login Button -->
        <Button 
            x:Name="LoginButton"
            Text="🔓 Login"
            BackgroundColor="#32b8c6"
            TextColor="White"
            Padding="12"
            CornerRadius="8"
            FontSize="16"
            FontAttributes="Bold"
            Clicked="OnLoginClicked"/>

        <!-- Loading Indicator -->
        <ActivityIndicator 
            x:Name="LoadingIndicator"
            IsRunning="False"
            IsVisible="False"
            Color="#32b8c6"
            Scale="1.5"/>

        <!-- Demo Credentials Info -->
        <Label 
            Text="Demo Credentials:&#10;admin / admin123&#10;customer / customer123"
            FontSize="11"
            TextColor="#999"
            HorizontalTextAlignment="Center"
            Margin="0,20,0,0"/>

    </VerticalStackLayout>

</ContentPage>
